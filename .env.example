# ─────────────────────────────────────────────────────────────────────────────
# PR Review Bot — Environment Configuration
# Copy this file to .env and fill in your values.
# ─────────────────────────────────────────────────────────────────────────────

# Ollama base URL (optional) — default: http://localhost:11434
# Ollama must be running (ollama serve) with your chosen model pulled.
# OLLAMA_BASE_URL=http://localhost:11434

# Ollama model name (optional) — default: llama3.2
# Pull with: ollama pull llama3.2 (or codellama, etc.)
# OLLAMA_MODEL=llama3.2

# GitHub Personal Access Token — use a fine-grained token with Pull requests (R/W) + Contents (Read)
# See README "Creating a GitHub token" for step-by-step creation.
GITHUB_TOKEN=ghp_...

# GitHub Webhook Secret — used to verify webhook payloads are from GitHub
# Generate a random string: openssl rand -hex 20
GITHUB_WEBHOOK_SECRET=your_random_secret_here

# The target repository to monitor (owner/repo) — the repo whose PRs will be reviewed (not this bot repo)
# Example: pavangeorge/my-project
GITHUB_REPO=owner/repo

# Port for the webhook receiver HTTP server
WEBHOOK_PORT=3000
