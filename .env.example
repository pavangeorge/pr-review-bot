# ─────────────────────────────────────────────────────────────────────────────
# PR Review Bot — Environment Configuration
# Copy this file to .env and fill in your values.
# ─────────────────────────────────────────────────────────────────────────────

# Anthropic API key — used by the Claude component to generate reviews
# Get yours at: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# GitHub Personal Access Token — needs repo + pull_requests read/write scopes
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_...

# GitHub Webhook Secret — used to verify webhook payloads are from GitHub
# Generate a random string: openssl rand -hex 20
GITHUB_WEBHOOK_SECRET=your_random_secret_here

# The GitHub repository to monitor (format: owner/repo)
# Example: pavangeorge/my-project
GITHUB_REPO=owner/repo

# Port for the webhook receiver HTTP server
WEBHOOK_PORT=3000
